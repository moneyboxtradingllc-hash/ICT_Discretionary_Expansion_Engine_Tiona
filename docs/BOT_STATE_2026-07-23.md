# STATE OF BOTH BOTS — reference as of 2026-07-23

## YOUR BOT — deterministic MNQ lane (`ICT_Discretionary_Expansion_Engine`, brain-off)

### Fixed & committed this week
| Commit | What |
|---|---|
| `755ca9c` | Risk-based sizing (fixed 15 → variable, $500/trade, 25pt cap) + fc0b fallback + zone-displacement trigger + FC-0B chase-conflict |
| `4004605` | Warm-up 400 → 2000 bars (400 was starving setup detection → 0 setups) |
| `4d717b7` | P&L tracking from broker (was recording opens, never closes → daily-loss ceiling blind) |
| `15581a7` | Date-aware session (fresh session on a new day; same-day stop can't be bypassed by relaunch) |
| `18a230f` | Decision window 09:30 → **14:00** |
| `dcfcd19` | Exhaustion veto now needs **≥2 timeframes** (was 1) + audit reason mirrored |
| `5b09053` | Dropped 1m as a zone source (95% of 1m stops below the survivability floor) |

### Results (2 sessions, N=2 — machine proven, edge NOT proven)
- **07-22:** LONG, stopped **−$470** (−1R) — right direction, stop too tight, nicked at the low then price ran to target
- **07-23:** SHORT (`liquidity_sweep_reversal`, all 20 gates), target **+$962** (+2R) — 16.75pt stop held and ran; fired at 13:26 (old window would have missed it)
- **Net +$492.** Today validated the exhaustion fix, window extension, risk-based sizing, and P&L tracking — all in one trade.
- N=2 proves nothing about edge. What's proven is the pipeline works end-to-end: authors a direction brain-off, sizes to risk, executes, protects, holds through noise, hits target, records P&L truthfully.

### Pending (not blocking — whenever)
- **5m zone anomaly** — median 4.3pt, *tighter than 3m's 19.3pt* (backwards). Likely a 5m zone-construction defect worth a look.
- **25pt cap amputates 51% of 15m stops** — worth revisiting on the right axis, since risk-based sizing already decouples stop width from dollar risk.
- **Within-timeframe stop variance** — source-TF selection can't fix it; ATR-relative survivability bands are the real cure.
- MNQ source-TF measured → decided to **keep {15m, 5m, 3m}** (own data said don't narrow further). Done.

### ⛔ Blocked on the Brain (returns ~7 business days — debit card)
- **THE binding wall.** Brain-off, the AB-2A firewall forbids structure from authoring direction; only **sweep+reclaim** or **PO3 delivery** can author. Pure structural continuation cannot — both trades this week were sweep-authored.
- When the Brain returns it takes the direction chair (AB-5B — `ai_brain` owns direction). Expect a **step-change up in frequency** — from 2 narrow mechanical paths to authoring on every qualified read.
- Action when it returns: turn it on, watch the first session or two closely, **re-run validation** — the bot will behave materially differently.

---

## TIONA'S BOT — CMST multi-symbol (`AI_MODE=internal`, 4 symbols)

### Established (her forensic work — exemplary discipline)
- The "silent VNRE drop" was **not** a broken handoff — she traced and retracted. Real blockers on the 61 execution_ready candidates: **54 had no authored direction**, **7 blocked by `max_stop_exceeded`**.
- Root cause of the stop problem: **two rails contradict** — a candle-anchored (volatility-scaled) stop generator governed by **fixed point-count** survivability bands. The flat 25pt cap is NQ doctrine applied to every symbol; **MYM is mathematically impossible** (floor 30 > cap 25).
- Stop dispersion: median in-band per symbol, but spread ~2× the band width; 1m 100% below floor.
- **Commit F shipped:** dropped 1m + per-symbol source-TF policy mechanism (table intentionally empty until measured).

### Pending (her sequence + what was just sent)
1. Run a session on Commit F → **re-measure residual** → then rule on per-symbol narrowing
2. **Apply the exhaustion ≥2-TF fix** — must mirror `_narrative_reason` or corrupt the audit trail
3. Three read-only checks flagged: the 7,141 unauthored count, P&L-ceiling blindness, session date-awareness
4. Run the **per-symbol measurement script** on all four instruments (set each from its own data)
5. Then: per-symbol caps + **ATR-relative bands** (the real cure for the two-rails contradiction)

### Not blocked on the Brain
Running internal/deterministic authorship — works differently than the brain-off path.

---

## SHARED — the architectural laws (both bots, same disease)
1. **Silent drops** — candidates dying with no recorded reason (the fingerprint behind every hard bug)
2. **One input vetoing a consensus** — exhaustion (1 TF), direction (1 neutral TF collapsing a 3-of-4)
3. **Fixed constants over variable instruments** — the 25pt cap across 20×-different point values
4. **Measured numbers don't transfer** — your 3m vs her 15m. Borrow the *laws*, never the *numbers*.

Written up in full as `ict_scalper_bot/MECHANICAL_SCALPER_BLUEPRINT.md` — the foundation for the clean mechanical rebuild.

---

## Immediate next actions
- **Nothing required today** — bot running, both fixes live, +2R on the board, 1 trade left in today's window.
- **When the card arrives:** Brain on → watch closely → re-run validation.
- **This week (brain-agnostic, high value):** the `ict_scalper_bot` Phase 0 rebuild is teed up in that thread.
- **Send Tiona the message** (exhaustion fix + 3 read-only checks + per-symbol measurement script).
