# DEPLOY-1 template — Topstep 50k evaluation profile (adapter is a stub; no live money)
owner_name: ""
broker: topstep
data_provider: topstep        # DEPLOY-2A — Topstep/ProjectX market data (NEVER Alpaca)
execution_provider: topstep   # DEPLOY-2C — Topstep runtime (NEVER alpaca_paper)
practice_only: true           # refuse live/funded routing
execution_enabled: false      # SAFE default: observe, place no orders
account_type: evaluation
account_id: ""
symbol: MNQU                  # DEPLOY-2B — Topstep instrument (Micro E-mini Nasdaq); never QQQ
contract_size: 1
session_windows:
  - "09:30-16:00"
execution_mode: paper          # DEPLOY-1 stays paper/shadow; never live
risk_profile:
  max_daily_loss: 500
  max_trades_per_day: 2
  risk_per_trade: 500
  risk_label: topstep_50k
divergence:
  confidence_calibration: 1.0
  scan_cadence_seconds: 60
  session_preference: rth
  allowed_playbooks: null      # null = all
  allowed_tools: null
